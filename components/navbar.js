import headerStyles from "../styles/navbar.module.css";

export default function profile(){
    return (
        <div className={headerStyles.navbar}>
            <a className={headerStyles.title}>COODING</a>
        </div>
    )
}